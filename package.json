{
  "name": "tsmithcode-software-architect-resume",
  "version": "0.1.0",
  "private": false,
  "description": "Standalone generator and canonical PDF for the TSmithCode software architect resume.",
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "generate": "node scripts/generate-resume.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "pnpm generate && pnpm test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
